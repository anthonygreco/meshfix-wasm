--- core-check-R.inc	2026-09-23 23:30:28.218768917 +0000
+++ core-check-G.inc	2026-09-23 23:37:27.372140436 +0000
@@ -1417,6 +1417,14 @@
                     ++result.holesFailed;
                     continue;
                 }
+                { // PROTOTYPE G: a loop that visits a vertex twice is two loops pinched at that vertex;
+                  // a fan across it hands PMP a triangle with a repeated vertex, which add_face
+                  // accepts and which corrupts the half-edge structure. Refuse it.
+                    std::vector<pmp::IndexType> ids; ids.reserve(loopVerts.size());
+                    for (auto lv : loopVerts) ids.push_back(lv.idx());
+                    std::sort(ids.begin(), ids.end());
+                    if (std::adjacent_find(ids.begin(), ids.end()) != ids.end()) { ++result.holesFailed; continue; }
+                }
 
                 // Leave deliberate geometry alone unless told otherwise. See
                 // looksDeliberate() for why the test is one-sided.
@@ -1439,6 +1447,7 @@
                 for (size_t i = 1; i + 1 < loopVerts.size(); ++i) {
                     try {
                         std::vector<pmp::Vertex> tri = {loopVerts[0], loopVerts[i], loopVerts[i + 1]};
+                        if (tri[0] == tri[1] || tri[1] == tri[2] || tri[0] == tri[2]) { anyFailed = true; continue; }
                         mesh_.add_face(tri);
                         ++facesAdded;
                     } catch (...) {
